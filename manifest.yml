applications:
  - name: SESMI-MM-NODEJS
    memory: 1G
    disk_quota: 1G
    buildpacks:
      - nodejs_buildpack
    command: npm run start:deploy
    services:
    - SESMI_CREDS
    default-route: true
    health-check-type: process
